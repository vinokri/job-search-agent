[project]
name = "credit-card-dashboard"
version = "0.1.0"
description = "Local-first multi-agent credit card dashboard."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
