[project]
name = "job-application-approval-agent"
version = "0.1.0"
description = "Shortlist jobs from target companies and stop at approval before application submission."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
