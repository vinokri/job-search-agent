[project]
name = "job-search-agent"
version = "0.1.0"
description = "Search target-company jobs, rank them against a candidate profile, and stop at approval."
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
